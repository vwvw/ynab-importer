{% extends "./layout/default" %}
{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                &nbsp;
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                <h1>{{ mainTitle }}</h1>
                <h2>{{ subTitle }}</h2>
            </div>
        </div>
        <div id="app">
            <download-status></download-status>
        </div>
    </div>
{% endblock %}
{% block scripts %}
    <script type="text/javascript">
        var downloadStatusUri = '{{ route('import.download.status') }}?downloadIdentifier={{ downloadIdentifier }}';
        var downloadStartUri = '{{ route('import.download.start') }}?downloadIdentifier={{ downloadIdentifier }}';
        var mappingUri = '{{ route('import.mapping.index') }}';
        var flushUri = '{{ route('flush') }}';
    </script>
    <script src="js/download.js" type="text/javascript"></script>
{% endblock %}
